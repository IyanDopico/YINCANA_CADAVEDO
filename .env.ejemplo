# Copia este archivo a .env (que no se commitea) y ajusta:
# - el PIN real del admin
# - tu uid:gid del host (id -u / id -g), para que ./datos no salga de root
YINCANA_PIN=1234
YINCANA_USER=1000:1000
